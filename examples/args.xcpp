#if !defined(__XCPP__)
#define __XCPP__ 0
#elif defined(__XCPP__)
#pragma once
#else
. xcpp.sh run "$0" "$@"
#endif

// Prints all command arguments
int args( strings arguments )
{
    for_i( sz, 0, arguments.size() )
    {
		cout << "args[" << i << "] " << arguments[i] << endl;
    }
    return 0;
}
